# 三维点云去噪 —— 依赖清单
#
# 目标环境：Ubuntu 22.04 + NVIDIA 4090 + CUDA 12.x + Python 3.9.25
# 云端完整验证：CUDA 12.2 + Python 3.9.25 + Jittor 1.3.10.0
# 安装：
#   python -m pip install -r requirements.txt
#
# Jittor 首次 import 会即时编译算子，需要机器上有 g++ 与对应 CUDA toolkit。
# 训练与推理全程只用 Jittor，不依赖 PyTorch。

# ---- 深度学习框架 ----
jittor==1.3.10

# ---- 数值与配置 ----
numpy==1.24.4
scipy==1.10.1
omegaconf==2.3.0
tqdm==4.66.4

# ---- 网格读取与采样（训练数据由 ShapeNet 网格采样得到）----
trimesh==4.4.3

# ---- 评测用（只在跑本地评测脚本时需要，训练与推理不依赖）----
# point-to-surface 距离需要精确的点到三角面距离，用这个库计算。
point-cloud-utils==0.30.4
